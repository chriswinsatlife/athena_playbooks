---
// Wrapper component for timeline styling
---

<div class="x-timeline">
	<slot />
</div>

<style>
	.x-timeline {
		display: flex;
		flex-direction: column;
		gap: 0;
		margin: 0 0 var(--space-l) 0;
		border: 1px solid var(--color-gray-200, #e5e7eb);
		border-radius: 12px;
		overflow: hidden;
		background: var(--color-white, #ffffff);
	}

	.x-timeline :global(.tweet:last-child) {
		border-bottom: none;
	}
</style>
